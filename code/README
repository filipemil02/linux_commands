În fișierul funcții.h se găsesc structurile și funcțiile ajutătoare în îndeplinirea taskurilor.Noile structuri au fost implementate cu toate campurile necesare descrise în enunț.Am făcut pentru aproape fiecare funcție câte 2:una pentru fișier și una pentru director.Functiile de construcție a nodului cât și inserarea în arborele binar de Căutare au fost luate din laborator/curs și Putin modificate pentru a se mula pe Exercițiu.Functiile de inserare în arborele binar de Căutare mai au un parametru în plus, parintele pentru a-l putea pune mai ușor în structuri.Afisarea arborilor este una de tip SRD.Functiile ce verifica existenta directorului/fișierului sunt recursive și în caz ca respectivul lucru este găsit, returnează 1 iar în caz contrar 0.
Funcțiile de recFis/Dir au ca scop construirea unui nou arbore binar de Căutare fără elementul specificat.
În main se face inițializarea părintelui director, root, așa cum se specifica în enunț și se începe citirea de la tastatura.Programul este împărțit în mai multe părți, fiecare avut ca task ceva diferit, după primul cuvânt citit de pe linie.La fiecare împărțire mă asigur ca ultimul caracter de la cel de-al doilea cuvânt este caracterul nul pentru a nu strica următoarele taskuri.Fiecare parte apelează cu argumentele necesare , funcțiile în urma cărora vor fi îndeplinite cernitele.
Funcția touch verifica prima data dacă cuvântul dat ca parametru este deja luat de alt director/fișier și în caz afirmativ anunță pe ecran acest lucru iar în celălat caz pune în arborele binar de Căutare al fișierului curent noul fișier.
Funcția ls parcurge în stil SRD atât directoarele și fișierele directorului curent și afișează pe ecran tot ce conține acesta.
Funcția mkdir este implementată la fel ca funcția touch numai ca astfel se Creează un nou director.
Funcția rm verfica în arborele de fișiere existenta unui fișier cu numele specificat și în urma rezultatului ori afișează pe ecran ca nu exista ori elimina acel fișier.
Funcția rmdir are aceeași implementare ca și rm numai ca aici se lucrează cu directoare.
Funcția cd se uita mai întâi la cuvântul dat ca parametru.In cazul în care acesta este "..", se dorește urcarea cu un nivel așa ca poziția inițială ia valorea părintelui poziției inițiale.In cazul în care nu se dorește acest lucru, se caută mai întâi în arbore dacă cumva exista directorul în care se dorește făcută mutarea și în caz negativ se afișează un mesaj indicând acest lucru.In cazul în care directorul a fost găsit, se caută în arborele binar de cautare pana când se găseste directorul iar directorul curent ia valorea respectivului.
Funcția pwd este recursiva dorindu-se mai întâi Afișarea directorului părințe și rând pe rând Afișarea tuturor directoarelor prin care se trece pana când se ajunge la directorul curent.
Funcția de findFis caută în tot arborele fișierul cu numele specificat, în mod recurent iar când acesta a fost găsit, se afișează pe ecran acest fapt cât și calea către acel fișier.In cazul în care fișierul nu a fost gășit, se afișează pe ecran un mesaj din care reiese acest lucru.
Funcția de findDir are aceeași implementare cu funcția precedenta doar ca este vorba de directoare.